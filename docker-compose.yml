services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tour_manager
      POSTGRES_USER: tour_dev
      POSTGRES_PASSWORD: tour_dev_password
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
